import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CompletarPerfilEmpresa2Page } from './completar-perfil-empresa-2.page';

describe('CompletarPerfilEmpresa2Page', () => {
  let component: CompletarPerfilEmpresa2Page;
  let fixture: ComponentFixture<CompletarPerfilEmpresa2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CompletarPerfilEmpresa2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
