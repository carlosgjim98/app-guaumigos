import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CompletarPerfil2Page } from './completar-perfil-2.page';

describe('CompletarPerfil2Page', () => {
  let component: CompletarPerfil2Page;
  let fixture: ComponentFixture<CompletarPerfil2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CompletarPerfil2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
